plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

run {
    standardInput = System.in
}

application {
    mainClass = 'Main'
}
tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}